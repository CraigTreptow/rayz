# name|description|env_vars
#
# Rust benchmark — compiles to native code with full optimizations (--release, LTO).
# Rayon provides true CPU-parallel rendering (no GIL).
sequential|Rust native (single-threaded, --release)|RAYON_NUM_THREADS=1
parallel|Rust native (multi-threaded, --release)|
