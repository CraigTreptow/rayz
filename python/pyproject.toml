[project]
name = "rayz"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.0",
    "behave>=1.2",
    "pytest>=8.0",
]

[dependency-groups]
dev = [
    "ruff>=0.9",
]

[tool.ruff]
line-length = 100
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "I"]
