# name|description|env_vars
#
# Crystal benchmark — compiles to native code via LLVM (--release flag).
# Crystal has no GIL; OS thread parallelism is genuine. Starting with
# sequential only; parallel variant will be added when rendering is threaded.
sequential|Crystal native (single-threaded, --release)|
